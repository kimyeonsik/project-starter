{
  "name": "project-starter",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": {
    "project-starter": "scripts/cli.mjs"
  },
  "scripts": {
    "test": "node --test"
  }
}
